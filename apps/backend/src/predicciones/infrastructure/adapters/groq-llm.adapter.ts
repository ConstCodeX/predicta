/**
 * @deprecated Ya no se usa. El proyecto usa exclusivamente Gemma vía
 * `@google/genai` (ver src/agentes/infrastructure/gemini-llm.adapter.ts).
 *
 * Este archivo se mantiene vacío a propósito para no depender del paquete
 * `openai` ni de credenciales de Groq. Puedes eliminarlo del repo.
 */
export {};
